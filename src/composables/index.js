/**
 * An example utility function.
 * @param {string} name - The name to greet.
 * @returns {string} A greeting message.
 */
export function useExampleUtil(name = 'World') {
  return `Hello, ${name}! This is an example utility.`;
}
